---
tags: 
 - rose-possible
 - recommendation
 - err
 - android-applicable
---
# ERR01-C. Use ferror() rather than errno to check for FILE stream errors

Use `ferror()` rather than `errno` to check whether an error has occurred on a file stream (for example, after a long chain of `stdio` calls). The `ferror()` function tests the error indicator for a specified stream and returns nonzero if and only if the error indicator is set for the stream.

## Noncompliant Code Example

Many [implementations](/sei-cert-c-coding-standard/back-matter/bb-definitions#BB.Definitions-implementation) of the `stdio` package adjust their behavior slightly if `stdout` is a terminal. To make the determination, these implementations perform some operation that fails (with `ENOTTY` ) if `stdout` is not a terminal. Although the output operation goes on to complete successfully, `errno` still contains `ENOTTY` . This behavior can be mildly confusing, but it is not strictly incorrect because it is meaningful for a program to inspect the contents of `errno` only after an error has been reported. More precisely, `errno` is meaningful only after a library function that sets `errno` on error has returned an error code.

::code-block{quality="bad"}
``` c
errno = 0;
printf("This\n");
printf("is\n");
printf("a\n");
printf("test.\n");
if (errno != 0) {
  fprintf(stderr, "printf failed: %s\n", strerror(errno));
}
```
::

## Compliant Solution

This compliant solution uses `ferror()` to detect an error. In addition, if an early call to `printf()` fails, later calls may modify `errno` , whether they fail or not, so the program cannot rely on being able to detect the root cause of the original failure if it waits until after a sequence of library calls to check.

::code-block{quality="good"}
``` c
printf("This\n");
printf("is\n");
printf("a\n");
printf("test.\n");
if (ferror(stdout)) {
  fprintf(stderr, "printf failed\n");
}
```
::

## Risk Assessment

Checking `errno` after multiple calls to library functions can lead to spurious error reporting, possibly resulting in incorrect program operation.

| Recommendation | Severity | Likelihood | Detectable | Repairable | Priority | Level |
| --- | --- | --- | --- | --- | --- | --- |
| ERR01-C | Low | Probable | Yes | Yes | <span style="color: #f1c40f;">**P6**</span> | <span style="color: #f1c40f;">**L2**</span> |

### Automated Detection

| Tool      | Version  | Checker | Description |
|-----------|----------|---------|-------------|
| <a href="/sei-cert-c-coding-standard/back-matter/ee-analyzers/eclair">ECLAIR</a> | <div class="content-wrapper">1.2</div> | **CC2.ERR01** | Fully implemented |
| <a href="/sei-cert-c-coding-standard/back-matter/ee-analyzers/ldra">LDRA tool suite</a> | <div class="content-wrapper">9.7.1</div> | 44 S | Enhanced Enforcement |
| <a href="/sei-cert-c-coding-standard/back-matter/ee-analyzers/parasoft">Parasoft C/C++test</a> | <div class="content-wrapper">2026.1</div> | **CERT_C-ERR01-a** | The error indicator 'errno' shall not be used |


### Related Vulnerabilities

Search for [vulnerabilities](/sei-cert-c-coding-standard/back-matter/bb-definitions#BB.Definitions-vulnerability) resulting from the violation of this rule on the [CERT website](https://www.kb.cert.org/vuls/search/?q=ERR01-C) .


## Bibliography

|                                                                                                       |                    |
|-------------------------------------------------------------------------------------------------------|--------------------|
| \[ [Horton 1990](/sei-cert-c-coding-standard/back-matter/aa-bibliography#AA.Bibliography-Horton90) \] | Section 14, p. 254 |
| \[ [Koenig 1989](/sei-cert-c-coding-standard/back-matter/aa-bibliography#AA.Bibliography-Koenig89) \] | Section 5.4, p. 73 |

  
