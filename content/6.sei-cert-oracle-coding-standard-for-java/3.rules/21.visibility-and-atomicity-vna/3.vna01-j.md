---
tags: 
 - vna
 - android-applicable
 - rule
---
# VNA01-J. Ensure visibility of shared references to immutable objects

A common misconception is that shared references to [immutable](http://Rule%20BB.%20Glossary#immutable) objects are immediately visible across multiple threads as soon as they are updated. For example, a developer can mistakenly believe that a class containing fields that refer only to immutable objects is itself immutable and consequently [thread-safe](/sei-cert-oracle-coding-standard-for-java/back-matter/rule-bb-glossary) .

Section 14.10.2, "Final Fields and Security," of *Java Programming Language* , Fourth Edition \[ [JPL 2006](/sei-cert-oracle-coding-standard-for-java/back-matter/rule-aa-references#RuleAA.References-JPL06) \] states:

> The problem is that, while the shared object is immutable, the reference used to access the shared object is itself shared and often mutable. Consequently, a correctly synchronized program must synchronize access to that shared reference, but often programs do not do this, because programmers do not recognize the need to do it.

![](plugins/servlet/confluence/placeholder/unknown-macro)

References to both immutable and mutable objects must be made visible to all the threads. Immutable objects can be shared safely among multiple threads. However, references to mutable objects can be made visible before the objects are fully constructed. [TSM03-J. Do not publish partially initialized objects](/sei-cert-oracle-coding-standard-for-java/rules/thread-safety-miscellaneous-tsm/tsm03-j) describes object construction and visibility issues specific to mutable objects.

## Noncompliant Code Example

This noncompliant code example consists of the immutable `Helper` class:

::code-block{quality="bad"}
``` java
// Immutable Helper
public final class Helper {
  private final int n;

  public Helper(int n) {
    this.n = n;
  }
  // ...
}
```
::

and a mutable `Foo` class:

::code-block{quality="bad"}
``` java
final class Foo {
  private Helper helper;

  public Helper getHelper() {
    return helper;
  }

  public void setHelper(int num) {
    helper = new Helper(num);
  }
}
```
::

The `getHelper()` method publishes the mutable `helper` field. Because the `Helper` class is [immutable](/sei-cert-oracle-coding-standard-for-java/back-matter/rule-bb-glossary#RuleBB.Glossary-immutable) , it cannot be changed after it is initialized.

![](plugins/servlet/confluence/placeholder/unknown-macro)

Furthermore, because `Helper` is immutable, it is always constructed properly before its reference is made visible, in compliance with [TSM03-J. Do not publish partially initialized objects](/sei-cert-oracle-coding-standard-for-java/rules/thread-safety-miscellaneous-tsm/tsm03-j) . Unfortunately, a separate thread could observe a stale reference in the `helper` field of the `Foo` class.

## Compliant Solution (Synchronization)

This compliant solution synchronizes the methods of the `Foo` class to ensure that no thread sees a stale `Helper` reference:

::code-block{quality="good"}
``` java
final class Foo {
  private Helper helper;

  public synchronized Helper getHelper() {
    return helper;
  }

  public synchronized void setHelper(int num) {
    helper = new Helper(num);
  }
}
```
::

The immutable `Helper` class remains unchanged.

## Compliant Solution ( `volatile` )

References to immutable member objects can be made visible by declaring them volatile:

::code-block{quality="good"}
``` java
final class Foo {
  private volatile Helper helper;

  public Helper getHelper() {
    return helper;
  }

  public void setHelper(int num) {
    helper = new Helper(num);
  }
}
```
::

The immutable `Helper` class remains unchanged.

## Compliant Solution ( `java.util.concurrent` Utilities)

This compliant solution wraps the mutable reference to the immutable `Helper` object within an `AtomicReference` wrapper that can be updated atomically:

::code-block{quality="good"}
``` java
final class Foo {
  private final AtomicReference<Helper> helperRef =
      new AtomicReference<Helper>();

  public Helper getHelper() {
    return helperRef.get();
  }

  public void setHelper(int num) {
    helperRef.set(new Helper(num));
  }
}
```
::

The immutable `Helper` class remains unchanged.

![](plugins/servlet/confluence/placeholder/unknown-macro)

## Risk Assessment

The incorrect assumption that classes that contain only references to immutable objects are themselves immutable can cause serious [thread-safety](/sei-cert-oracle-coding-standard-for-java/back-matter/rule-bb-glossary#RuleBB.Glossary-thread) issues.

<table>
  <thead>
    <tr>
      <th>Rule</th>
      <th>Severity</th>
      <th>Likelihood</th>
      <th>Detectable</th>
      <th>Repairable</th>
      <th>Priority</th>
      <th>Level</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>VNA01-J</td>
      <td>Low</td>
      <td>Probable</td>
      <td>Yes</td>
      <td>No</td>
      <td style="color: #27ae60;">**P4**</td>
      <td style="color: #27ae60;">**L3**</td>
    </tr>
  </tbody>
</table>
### Automated Detection

Some static analysis tools are capable of detecting violations of this rule.

<table class="wrapped"><tbody><tr class="header"><th>Tool</th><th>Version</th><th>Checker</th><th>Description</th></tr><tr class="odd"><td><a href="/sei-cert-oracle-coding-standard-for-java/back-matter/rule-or-rec-cc-analyzers/klocwork">Klocwork</a></td><td><div class="content-wrapper"><p>2025.2</p></div></td><td><strong>SV.SHARED.VAR</strong></td><td><br />
</td></tr><tr class="even"><td><a href="/sei-cert-oracle-coding-standard-for-java/back-matter/rule-or-rec-cc-analyzers/parasoft">Parasoft Jtest</a></td><td><div class="content-wrapper">2025.2</div></td><td><p><strong>CERT.VNA01.SGAS</strong></p></td><td>Use the synchronized keyword on both the getter and setter methods, or on neither</td></tr><tr class="odd"><td><a href="/sei-cert-oracle-coding-standard-for-java/back-matter/rule-or-rec-cc-analyzers/threadsafe">ThreadSafe</a></td><td><div class="content-wrapper">1.3</div></td><td><p><strong>CCE_SL_INCONSISTENT</strong><br />
<strong>CCE_CC_CALLBACK_ACCESS</strong><br />
<strong>CCE_SL_MIXED</strong><br />
<strong>CCE_SL_INCONSISTENT_COL</strong><br />
<strong>CCE_SL_MIXED_COL</strong><br />
<strong>CCE_CC_UNSAFE_CONTENT</strong></p></td><td>Implemented</td></tr><tr class="even"><td><a href="/sei-cert-oracle-coding-standard-for-java/back-matter/rule-or-rec-cc-analyzers/sonarqube">SonarQube</a></td><td><div class="content-wrapper">9.9</div></td><td><strong><a href="https://rules.sonarsource.com/java/RSPEC-2886">S2886</a></strong></td><td><a href="https://rules.sonarsource.com/java/RSPEC-2886">Getters and setters should be synchronized in pairs</a></td></tr></tbody></table>

  

## Bibliography

<table class="wrapped"><colgroup><col style="width: 50%" /><col style="width: 50%" /></colgroup><tbody><tr class="odd"><td><p>[ <a href="/sei-cert-oracle-coding-standard-for-java/back-matter/rule-aa-references#RuleAA.References-API14">API 2014</a> ]</p></td><td><p><br />
</p></td></tr><tr class="even"><td><p>[ <a href="/sei-cert-oracle-coding-standard-for-java/back-matter/rule-aa-references#RuleAA.References-JPL06">JPL 2006</a> ]</p></td><td><p>Section 14.10.2, "Final Fields and Security"</p></td></tr></tbody></table>

## Issue Tracking

## Review List

###### 50% of the tasks completed

|     |                                                                                                                              |        |
|-----|------------------------------------------------------------------------------------------------------------------------------|-------:|
|     | "Unfortunately, a separate thread could **can** observe a stale reference in the helper field of the Foo class."             | Medium |
|     | "This compliant solution synchronizes the methods of **class** Foo class " (it sounds strange with class occuring after Foo) | Medium |

  

------------------------------------------------------------------------

<a href="/sei-cert-oracle-coding-standard-for-java/rules/visibility-and-atomicity-vna/vna00-j"><img src="/attachments/88487702/88497198.png" /></a> <a href="/sei-cert-oracle-coding-standard-for-java/rules/visibility-and-atomicity-vna/"><img src="/attachments/88487702/88497196.png" /></a> <a href="/sei-cert-oracle-coding-standard-for-java/rules/visibility-and-atomicity-vna/vna02-j"><img src="/attachments/88487702/88497197.png" /></a>
