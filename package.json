{
  "name": "secure-coding-standards",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "NODE_OPTIONS='--max-old-space-size=6144' npx nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "change-registry-in-package-lock": "./scripts/update-package-lock.sh"
  },
  "dependencies": {
    "@cmu-sei/sds-documentation-site": "^1.7.0",
    "nuxt": "^4.5.2",
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "vue-router": "^5.3.0"
  },
  "overrides": {
    "commander": "^13.1.0"
  }
}
